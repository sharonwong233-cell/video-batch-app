# 课程剪辑 FrameFlow 工程整理 — 2026-09-22

应用仍为1.5.1，项目格式仍为3。本次为目录、入口、命名与开发文档维护，不新增视频功能。

## 变更

- 根README、应用界面/窗口、安装器显示标题以“课程剪辑 FrameFlow”开头；保留打码、拼接、网课和快速分段关键词。
- 根package.json为唯一启动入口，main指向src/main.js；原高保真原型HTML改为src/app.html，实际功能不变。
- 脚本迁至scripts/，测试迁至tests/，安装配置迁至build/，文档与冻结历史迁至docs/。
- 更新开发启动、渲染资源、主进程引擎、原生测试、便携同步和NSIS路径。
- 新增Windows从源码下载并校验依赖的入口，不再必须取得旧的完整迁移ZIP。
- 保持FrameFlow内部应用名、用户数据位置和安装注册表标识，避免仅因更名丢失项目恢复入口。
- 安装产物统一输出dist/，测试产物使用.build-cache/或reports/，不纳入源码提交。

## 已验证

- 全部源码/脚本/测试JS语法检查通过。
- 根入口、脚本目标、渲染脚本引用、版本一致性和项目格式检查通过。
- 项目保存回归通过：图片字典与区域往返、取消不改文件、无效格式拒绝、旧格式迁移；文件对话框为模拟。
- 40项真实视频引擎测试通过：预览、打码时间范围、混合比例补边、无损复制、逐帧/逐包核验、取消、不覆盖原视频、异常回退与用时冻结。
- 引擎与项目模型源码相对整理前保持不变。
- 使用新目录的源码同步及NSIS脚本成功编译Windows安装器；PE结构、归档校验和输出SHA-256检查通过。产物为dist/课程剪辑-FrameFlow-v1.5.1-Windows-x64-Setup.exe，159092973字节；构建记录在本地reports/V1.5.1安装包/。未声称Windows安装运行已通过。

## 验证边界

上述执行环境为macOS。Windows首次在线下载/解压流程、桌面交互、安装升级和卸载尚需Windows实测；不把本机检查说成Windows真机验收。当前未重跑浏览器几何测试，不将历史2210项结果冒充本次结果。

## 历史保留

远端旧布局保存到archive/before-layout-20260922分支，基点cc1b67f08eef3f089cf1a0fc6103c1cacf8eeea4。主分支整理提交保留该提交为父，不强推、不改写旧历史。原本机工程与安装包不删除，新本地工程位于“课程剪辑-FrameFlow”目录。

GitHub只同步源码、文档和文本记录，不含运行时、安装包、用户数据与历史媒体。安装器仍需单独发布，README明确源码ZIP不是安装程序。
